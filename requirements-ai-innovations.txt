# Tienda Eaciot — AI Innovation Dependencies
# Add these to existing requirements.txt for the 15 innovations

# ── Vector DB & Semantic Search (#1 RAG, #13)
chromadb==0.5.0
qdrant-client==1.9.0
meilisearch==0.31.0

# ── Agent Frameworks (#2 Agentic Checkout)
langgraph==0.1.0
# Note: langgraph requires langchain-core
langchain-core==0.2.0

# ── Dynamic Pricing (#3) 
nevergrad==1.0.0
scikit-opt==0.6.6
scrapy==2.11.0          # competitor price scraping

# ── Personalization (#4)
implicit==0.7.2
scipy==1.12.0            # required by implicit

# ── Visual Search (#5)
open-clip-torch==2.24.0
torch==2.2.0             # CPU-only for inference
Pillow==10.3.0           # already in project

# ── Product Content (#6)
keybert==0.8.3
instructor==1.2.0

# ── Autonomous Campaigns (#7)
apscheduler==3.10.4      # cron-style scheduling
# n8n runs as separate Docker service — no pip dep

# ── Voice Commerce (#10)
faster-whisper==1.0.3

# ── Fraud Detection (#11)
pyod==1.1.3

# ── Inventory Forecasting (#12)
statsforecast==1.7.0
# prophet requires pystan — heavier, use statsforecast instead

# ── Photo Enhancement (#14)
rembg==2.0.57

# ── AI Analytics (#15)
vanna==0.5.0
matplotlib==3.8.3
plotly==5.19.0

# ── Monitoring
evidently==0.4.0
mlflow==2.11.0
