fastapi==0.110.0
starlette==0.36.3
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]==2.0.29
greenlet==3.0.3
alembic==1.13.1
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
httpx==0.27.0
ollama==0.1.8
stripe==8.11.0
jinja2==3.1.6
python-multipart==0.0.20
pydantic[email]==2.6.4
pydantic-settings==2.2.1
python-dotenv==1.0.1
pytest==8.1.1
pytest-asyncio==0.23.5
aiosmtplib==3.0.1
email-validator==2.1.0
aiosqlite==0.20.0

# CFDI 4.0 nativo (generación + firma + timbrado vía PAC)
satcfdi==4.4.7

# RAG es OPCIONAL: `pip install chromadb` para habilitar la búsqueda semántica
# del asistente. Sin él, el producto advisor usa keyword search automáticamente.
# No se incluye por defecto: chromadb es pesado y en Render free el filesystem
# es efímero, por lo que no aportaría persistencia real.
# chromadb==1.5.9
